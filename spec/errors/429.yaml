description: Rate Limit Exceeded
content:
  application/json:
    schema:
      type: object
      properties:
        error_msg:
          type: string
      required:
      - error_msg
    example:
      error_msg: "You are doing this too much!"